apiVersion: v1
kind: Service
metadata:
  name: {{ include "ravi_helm.fullname" . }}
  labels:
    {{- include "ravi_helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ravi_helm.selectorLabels" . | nindent 4 }}
